name: Test FlowBox

on:
  push:
    branches: [main]
  pull_request:

jobs:
  swift-test:
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Run Swift tests
        working-directory: .
        run: swift test
