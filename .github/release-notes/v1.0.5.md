## FlowBox v1.0.5 — 修复设置无法保存 / 流畅滚动与人脸锁屏默认开启

v1.0.5 是一次关键修复 + 体验调整：

- **修复「设置改了重启就丢」**。根因：配置文件原来放在 Finder 扩展的沙盒容器里，macOS 新版（27）起系统拒绝宿主 App 读写扩展容器（errno 1），而写入失败被静默吞掉——于是每次勾选开关、改模板、换快捷键都只活在内存里，重启全部回退。表现为「每次打开 app，流畅滚动/人脸锁屏都没勾选」。
- **配置文件搬家到 `~/Library/Application Support/FlowBox/`**（宿主 App 唯一写入方）。Finder 扩展改用只读沙盒例外读取同一路径；写入失败现在会留痕到 `/tmp/flowbox-config-debug.log`，不再静默。
- **老配置自动迁移**。扩展容器里的旧配置只有扩展自己读得到，升级后首次运行时扩展会把它递交给宿主落盘（`flowbox://cfgsync` 内部通道），模板、快捷键、滚轮参数等全部保留。
- **流畅滚动（滚轮增强）与「离开自动锁屏」默认开启**：新装机开箱即用；从旧版本升级的用户会在首次启动时自动翻开一次（此后在设置里手动关闭的，会一直保持关闭）。
- 首次启动会按需请求「辅助功能」（流畅滚动）与「摄像头」（人脸锁屏）授权，拒绝后对应功能静默停用，可在系统设置里补授权。

Bilingual summary:

- **Fixed "settings revert after restart".** The config file lived inside the Finder extension's sandbox container; on newer macOS (27) the host app is denied any read/write into that container (errno 1), and the failure was silently swallowed — every toggle, template or hotkey change lived only in memory and vanished on relaunch.
- **Config relocated to `~/Library/Application Support/FlowBox/`** (the host app is the sole writer). The Finder extension reads it via a read-only sandbox exception; write failures are now logged to `/tmp/flowbox-config-debug.log` instead of being silent.
- **Automatic migration of old configs.** Only the extension can still read the old container copy, so on first launch after upgrading it hands the file to the host over an internal `flowbox://cfgsync` channel — templates, hotkeys and scroll tuning are preserved.
- **Smooth scrolling and presence auto-lock are now on by default** for fresh installs; upgraders get both flipped on once at first launch (an explicit opt-out afterwards persists).
- First launch requests Accessibility (smooth scrolling) and Camera (presence lock) as needed; declined features stay silently off until granted in System Settings.

### 安装 / Install

1. 下载下方 FlowBox.dmg，把 FlowBox.app 拖进 Applications。
2. 首次打开请在"应用程序"里右键点击 FlowBox →"打开"（自签名包，直接双击会报无法验证开发者，只需做一次）。
3. 点菜单栏图标 →「启用扩展」，在系统设置中打开 FlowBox 的 Finder 扩展开关；按需授予辅助功能、屏幕录制、摄像头、麦克风权限。

### 说明 / Notes

- Bundle ID: net.ai2048.flowbox；版本 1.0.5
- 从 v1.0.4 及更早版本升级：老配置自动迁移；如遇异常，旧文件仍保留在 `~/Library/Containers/net.ai2048.flowbox.ext/` 内未删除。
- 主人脸特征向量只存本机配置，比对只回答"是主人/不是主人"，不识别具体是谁，不联网不上传。
- 本地优先：当前源码无网络上传、遥测或第三方分析 SDK。
- 已知限制：自签名未公证；光线大变/遮挡可能误判陌生人，建议换环境后重新注册。
- 官网：https://ai2048.net/flowbox/ ｜ 源码：dev 分支
