## FlowBox v1.0.4 — 隐藏图标显示真实应用名 / 点击响应与列表准确性修复

在 v1.0.3 基础上修复菜单栏收纳的一组问题：

- **下拉里的隐藏图标现在显示真实应用名（含被刘海盖住的）**。此前匿名图标只能显示「菜单栏图标 <窗口号>」。新方案读取每个应用的 `AXExtrasMenuBar` 属性：它的子项就是该应用的状态项按钮，**即使图标被刘海或容量挤掉、屏幕上没有渲染，该属性依然报出真实布局坐标**（与窗口坐标误差 ≤1pt）。按坐标双向最近匹配即可直接得到 bundle id，于是下拉里显示的是 **微信 / 聚焦 / aTrust / stockdb** 这样的真名。不再依赖「腾位点射」或位置键猜测。
- **修复「点箭头有时没反应，得点好几次」**。两个真实成因：① 启动后有一轮旧的腾位辨认会把箭头摘下约 1 秒（AXExtras 具名后已无必要，删除）；② 菜单弹出路径在主线程同步读取 `defaults`（0.4–1.2 秒），期间菜单打不开，用户再点一下反而把刚弹出的菜单关掉（改为旧值顶住 + 后台刷新，弹出路径不再有任何同步等待）。实测箭头在场且菜单关闭时点击稳定打开，菜单构建 12–25ms。
- **修复「刚启动的应用不出现在下拉里」**。菜单栏满时，新启动应用的图标会保留一个落在可见区内的**幻影坐标**（位置看着在里面、屏幕上零像素）。旧判据只看坐标，于是把它当成了可见而不列出。现在可见性要求**真正渲染且坐标在可见区内**两个条件同时成立，其余一律当作隐藏列出。
- **多屏相关修复（v1.0.3 之后合入、本次一并发布）**：被挤判定锚定**带刘海的屏**（`CGMainDisplayID` 会跟随键盘焦点跳变）；副屏重连导致镜像窗口整体重建时，用身份缓存顶住名字断供；同宽组在两屏顺序相反时用**可见项的 AX 真身锚点**校正，避免 QQ / Spotlight 这类同宽项身份对调。

Bilingual summary:

- **Hidden rows now show the real app name, even under the notch.** Anonymous items used to fall back to "菜单栏图标 <windowNumber>". FlowBox now reads each app's `AXExtrasMenuBar` attribute, whose children are that app's status-item buttons — it reports their **layout coordinates even when the icon is squeezed out by the notch/capacity and never rendered** (≤1pt off the CGWindow X). Matching by nearest x yields the bundle id directly, so rows read **WeChat / Spotlight / aTrust / stockdb**. No more squeeze-probing or position-key guessing.
- **Fixed "clicking the chevron sometimes does nothing".** Two real causes: a leftover launch-time identification pass removed the arrow for ~1s (now unnecessary thanks to AXExtras naming and removed), and the menu-open path forked `defaults` synchronously on the main thread (0.4–1.2s, menu unresponsive; the user's second click then closed the just-opened menu). The menu path now serves stale-while-revalidate with zero synchronous waits; measured menu builds take 12–25ms.
- **Fixed "a newly launched app is missing from the dropdown".** When the menu bar is full, a squeezed item can keep a **stale x inside the visible band** while nothing is rendered on screen. The old rule trusted x alone. Visibility now requires **both** an on-screen flag and an in-band x; everything else is listed as hidden.
- **Multi-display fixes merged since v1.0.3**: anchor the squeezed-item scan to the **notched screen** (`CGMainDisplayID` follows keyboard focus); keep identities through secondary-display reconnects via the identity cache; reconcile same-width pairs with **axis anchors from visible items** so items like QQ/Spotlight can't be swapped.

### 安装 / Install

1. 下载下方 FlowBox.dmg，把 FlowBox.app 拖进 Applications。
2. 首次打开请在"应用程序"里右键点击 FlowBox →"打开"（自签名包，直接双击会报无法验证开发者，只需做一次）。
3. 点菜单栏图标 →「启用扩展」，在系统设置中打开 FlowBox 的 Finder 扩展开关；按需授予辅助功能、屏幕录制、摄像头、麦克风权限。

### 说明 / Notes

- Bundle ID: net.ai2048.flowbox；版本 1.0.4
- 主人脸特征向量只存本机配置，比对只回答"是主人/不是主人"，不识别具体是谁，不联网不上传。
- 本地优先：当前源码无网络上传、遥测或第三方分析 SDK。
- 已知限制：自签名未公证；光线大变/遮挡可能误判陌生人，建议换环境后重新注册。
- 官网：https://ai2048.net/flowbox/ ｜ 源码：dev 分支
